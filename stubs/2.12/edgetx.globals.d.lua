---@meta edgetx

--- AUTO-GENERATED BY edgetx-lua-gen (https://github.com/JeffreyChix/edgetx-lua-gen) — DO NOT EDIT MANUALLY.
--- EdgeTX Lua API stubs for lua-language-server (LuaLS)
--- Global functions available in all EdgeTX Lua scripts

--- Return an iterator listing all the files and directories name in a directory
--- ### Example
--- 
--- ```lua
---   for fname in dir(".") do
---     print(fname)
---   end
--- ```
---
--- **Since:** 2.5.0
---@param directory string #Working directory
function dir(directory) end
---@class (exact) General_fstat_Return
---@field year number #year
---@field mon number #month
---@field day number #day of month
---@field hour number #hours
---@field hour12 number #hours in US format
---@field min number #minutes
---@field sec number #seconds
---@field suffix string #(text) am or pm

--- Checks the existence of file or directory.
---  If not exist, return nil.
---  If exist, return the object information.
--- ### Example
--- 
--- ```lua
---   info = fstat("radio")
---   if info ~= nil then
---     if (info.attrib == AM_DIR) then
---       print("is a directory")
---     end
--- 
---     size = info.size
---     time = info.time
---   end
--- ```
---
--- **Since:** 2.5.0
---@param path string #path to the object
---@return General_fstat_Return table #object info, table elements:
--- 'size' (number) file size
--- 'attrib' (number) file attribute flags
--- 'time' (table) table with last time modified date and times, table elements:
--- `year` (number) year
--- `mon` (number) month
--- `day` (number) day of month
--- `hour` (number) hours
--- `hour12` (number) hours in US format
--- `min` (number) minutes
--- `sec` (number) seconds
--- `suffix` (text) am or pm
function fstat(path) end
--- Returns FRESULT (e.g. 0=OK, 4=File not found, 5=Path not found, 6=Path invalid)
--- ### Example
--- 
--- ```lua
---   if del("/SCRIPTS/TOOLS/deleteme.txt") == 0 then
---      - - successfully deleted file
---   else
---      - - failed to delete file
---   end
--- ```
---
--- **Since:** 2.9.0
---@param file_or_directory any
function del(file_or_directory) end
--- Change the working directory
---
--- **Since:** 2.3.0
---@param directory string #New working directory
function chdir(directory) end
--- Create a directory
---
--- **Since:** 2.11.0
---@param directory string #Directory to create
function mkdir(directory) end
--- Rename a file or directory
---   If the file or directory is being moved to a new parent directory, then the new parent
---   directory must already exist.
---
--- **Since:** 2.11.0
---@param from_path string #Current path to rename
---@param to_path string #Path to rename to
function rename(from_path, to_path) end
--- Return OpenTX version
--- ### Example
--- 
--- This example also runs in OpenTX versions where the function returned only one value:
--- 
--- ```lua
--- local function run(event)
---   local ver, radio, maj, minor, rev, osname = getVersion()
---   print("version: "..ver)
---   if radio then print ("radio: "..radio) end
---   if maj then print ("maj: "..maj) end
---   if minor then print ("minor: "..minor) end
---   if rev then print ("rev: "..rev) end
---   if osname then print ("osname: "..osname) end
---   return 1
--- end
--- 
--- return {  run=run }
--- ```
--- Output of the above script in simulator:
--- ```
--- version: 2.4.0
--- radio: tx16s-simu
--- maj: 2
--- minor: 4
--- rev: 0
--- osname: EdgeTX
--- ```
---
--- **Since:** 2.0.0
---@return string string #OpenTX version (ie "2.1.5")
---@return string|number multiple #values (available since 2.1.7):
--- (string) OpenTX version (ie "2.1.5")
--- (string) radio type: `x12s`, `x10`, `x9e`, `x9d+`, `x9d` or `x7`.
--- If running in simulator the "-simu" is added
--- (number) major version (ie 2 if version 2.1.5)
--- (number) minor version (ie 1 if version 2.1.5)
--- (number) revision number (ie 5 if version 2.1.5)
--- Since EdgeTX 2.4.0, sixth value added
--- (string) OS name (i.e. EdgeTX or nil if OpenTX)
function getVersion() end
--- Return the time since the radio was started in multiple of 10ms
---
--- **Since:** 2.0.0
---@return number number #Number of 10ms ticks since the radio was started Example:
--- run time: 12.54 seconds, return value: 1254
--- 
--- The timer internally uses a 32-bit counter which is enough for 497 days so
--- overflows will not happen.
function getTime() end
---@class (exact) General_getDateTime_Return
---@field year number #year
---@field mon number #month
---@field day number #day of month
---@field hour number #hours
---@field hour12 number #hours in US format
---@field min number #minutes
---@field sec number #seconds
---@field suffix string #(text) am or pm

--- Return current system date and time that is kept by the RTC unit
---
---@return General_getDateTime_Return table #current date and time, table elements:
--- `year` (number) year
--- `mon` (number) month
--- `day` (number) day of month
--- `hour` (number) hours
--- `hour12` (number) hours in US format
--- `min` (number) minutes
--- `sec` (number) seconds
--- `suffix` (text) am or pm
function getDateTime() end
--- Return current RTC system date as unix timstamp (in seconds since 1. Jan 1970)
--- 
--- Please note the RTC timestamp is kept internally as a 32bit integer, which will overflow
--- in 2038.
---
---@return number number #Number of seconds elapsed since 1. Jan 1970
function getRtcTime() end
---@class (exact) General_getFieldInfo_Return1
---@field id number #field identifier
---@field name string #field name
---@field desc string #field description
---@field unit number #unit identifier [Full list](../appendix/units.html)

--- Return detailed information about field (source)
--- 
--- The list of valid sources is available:
--- 
--- | OpenTX Version | Radio |
--- |- -- -- -- -- -- -- -- -|- -- -- --|
--- | 2.0 | [all](http://downloads-20.open-tx.org/firmware/lua_fields.txt) |
--- | 2.1 | [X9D and X9D+](http://downloads-21.open-tx.org/firmware/lua_fields_taranis.txt), [X9E](http://downloads-21.open-tx.org/firmware/lua_fields_taranis_x9e.txt) |
--- | 2.2 | [X9D and X9D+](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9e.txt), [Horus](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x12s.txt) |
--- | 2.3 | [X9D and X9D+](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9e.txt), [X7](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x7.txt), [Horus](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x12s.txt) |
---
--- **Since:** 2.0.8
---@param source number|string #can be an index (number) (which was obtained by `getFieldInfo` or `getSourceIndex`) or a name (string) of the source.
---@return General_getFieldInfo_Return1 table #information about requested field, table elements:
--- `id`   (number) field identifier
--- `name` (string) field name
--- `desc` (string) field description
--- `unit` (number) unit identifier [Full list](../appendix/units.html)
---@return nil _nil #the requested field was not found
function getFieldInfo(source) end
---@class (exact) General_getValue_Return2
---@field lat number #latitude, positive is North
---@field lon number #longitude, positive is East
---@field pilot-lat number #pilot latitude, positive is North
---@field pilot-lon number #pilot longitude, positive is East

--- Returns the value of a source.
--- 
--- The list of fixed sources:
--- 
--- | OpenTX Version | Radio |
--- |- -- -- -- -- -- -- -- -|- -- -- --|
--- | 2.0 | [all](http://downloads-20.open-tx.org/firmware/lua_fields.txt) |
--- | 2.1 | [X9D and X9D+](http://downloads-21.open-tx.org/firmware/lua_fields_taranis.txt), [X9E](http://downloads-21.open-tx.org/firmware/lua_fields_taranis_x9e.txt) |
--- | 2.2 | [X9D and X9D+](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9e.txt), [Horus](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x12s.txt) |
--- | 2.3 | [X9D and X9D+](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9e.txt), [X7](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x7.txt), [Horus](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x12s.txt) |
--- 
--- In OpenTX 2.1.x the telemetry sources no longer have a predefined name.
--- To get a telemetry value simply use it's sensor name. For example:
--- Altitude sensor has a name "Alt"
--- to get the current altitude use the source "Alt"
--- to get the minimum altitude use the source "Alt-", to get the maximum use "Alt+"
---
--- **Since:** 2.0.0
--- > **Notice:** Getting a value by its numerical identifier is faster then by its name.
--- > While `Cels` sensor returns current values of all cells in a table, a `Cels+` or
--- > `Cels-` will return a single value - the maximum or minimum Cels value.
---@param source number|string #can be an index (number) (which was obtained by `getFieldInfo` or `getSourceIndex`) or a name (string) of the source.
---@return number value #current source value (number). Zero is returned for:
--- non-existing sources
--- for all telemetry source when the telemetry stream is not received
--- far all non allowed sensors while FAI MODE is active
---@return General_getValue_Return2 table #GPS position is returned in a table:
--- `lat` (number) latitude, positive is North
--- `lon` (number) longitude, positive is East
--- `pilot-lat` (number) pilot latitude, positive is North
--- `pilot-lon` (number) pilot longitude, positive is East
---@return table table #GPS date/time, see getDateTime()
---@return table table #Cells are returned in a table
--- (except where no cells were detected in which
--- case the returned value is 0):
--- table has one item for each detected cell:
--- key (number) cell number (1 to number of cells)
--- value (number) current cell voltage
function getValue(source) end
--- Returns the value of a source. Superseeds getValue.
--- 
--- The list of fixed sources:
--- 
--- | OpenTX Version | Radio |
--- |- -- -- -- -- -- -- -- -|- -- -- --|
--- | 2.0 | [all](http://downloads-20.open-tx.org/firmware/lua_fields.txt) |
--- | 2.1 | [X9D and X9D+](http://downloads-21.open-tx.org/firmware/lua_fields_taranis.txt), [X9E](http://downloads-21.open-tx.org/firmware/lua_fields_taranis_x9e.txt) |
--- | 2.2 | [X9D and X9D+](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x9e.txt), [Horus](http://downloads.open-tx.org/2.2/release/firmware/lua_fields_x12s.txt) |
--- | 2.3 | [X9D and X9D+](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9d.txt), [X9E](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x9e.txt), [X7](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x7.txt), [Horus](http://downloads.open-tx.org/2.3/release/firmware/lua_fields_x12s.txt) |
--- 
--- In OpenTX 2.1.x the telemetry sources no longer have a predefined name.
--- To get a telemetry value simply use it's sensor name. For example:
--- Altitude sensor has a name "Alt"
--- to get the current altitude use the source "Alt"
--- to get the minimum altitude use the source "Alt-", to get the maximum use "Alt+"
---
--- **Since:** 2.0.0
--- > **Notice:** Getting a value by its numerical identifier is much faster than by its name.
--- > While `Cels` sensor returns current values of all cells in a table, a `Cels+` or
--- > `Cels-` will return a single value - the maximum or minimum Cels value.
---@param source number|string #can be an index (number) (which was obtained by `getFieldInfo` or `getSourceIndex`) or a name (string) of the source.
---@return number value #current source value (number), or last known telemetry item value.
--- 
--- value is nil for non-existing sources. all non-allowed sensors while FAI MODE is active, or if a telemetry item value has never been received
--- 
--- value is a table for GPS position:
--- 
--- `lat` (number) latitude, positive is North
--- `lon` (number) longitude, positive is East
--- `pilot-lat` (number) pilot latitude, positive is North
--- `pilot-lon` (number) pilot longitude, positive is East
--- 
--- value is a table for date/time, see getDateTime()
--- 
--- value is a table for battery cells(except where no cells were detected in which case the returned value is 0):
--- table has one item for each detected cell:
--- key (number) cell number (1 to number of cells)
--- value (number) current cell voltage
---@return boolean isCurrent #is true for telemetry sources that are within the "Sensor Lost" duration and telemetry is streaming . Always true for non-telemetry items.
---@return boolean isFresh #is true for telemetry sources which have been recently updated and telemetry is streaming. Always true for non-telemetry items.
function getSourceValue(source) end
--- Return rotary encoder current speed
---
--- **Since:** 2.3.10
---@return number number #in list: ROTENC_LOWSPEED, ROTENC_MIDSPEED, ROTENC_HIGHSPEED
---         returns ROTENC_LOWSPEED on radio without rotary encoder
function getRotEncSpeed() end
--- Return rotary encoder mode
---
--- **Since:** 2.8.0
---@return number number #in list: Normal = 0, Both V and H inverted = 1, V-N = 2, V-A = 3
---   return 0 on radio without rotary encoder
function getRotEncMode() end
--- Pops a received SPORT packet from the queue. Please note that only packets using a data ID within 0x5000 to 0x50FF
--- (frame ID == 0x10), as well as packets with a frame ID equal 0x32 (regardless of the data ID) will be passed to
--- the LUA telemetry receive queue.
---
--- **Since:** 2.2.0
---@return nil _nil #queue does not contain any (or enough) bytes to form a whole packet
---@return number multiple #returns 4 values:
--- sensor ID (number)
--- frame ID (number)
--- data ID (number)
--- value (number)
function sportTelemetryPop() end
--- This functions allows for sending SPORT telemetry data toward the receiver,
--- and more generally, to anything connected SPORT bus on the receiver or transmitter.
--- 
--- When called without parameters, it will only return the status of the output buffer without sending anything.
---
--- **Since:** 2.2.0
---@param sensorId? any #physical sensor ID
---@param frameId? any #frame ID
---@param dataId? any #data ID
---@param value? number #value
---@return boolean boolean #data queued in output buffer or not.
---@return nil _nil #incorrect telemetry protocol.
function sportTelemetryPush(sensorId, frameId, dataId, value) end
--- This functions allows for sending SPORT / ACCESS telemetry data toward the receiver,
--- and more generally, to anything connected SPORT bus on the receiver or transmitter.
--- 
--- When called without parameters, it will only return the status of the output buffer without sending anything.
---
--- **Since:** 2.3
---@param module? number #module index (0 = internal, 1 = external)
---@param rxUid? number #receiver index
---@param sensorId? any #physical sensor ID
---@param frameId? any #frame ID
---@param dataId? any #data ID
---@param value? number #value
---@return boolean boolean #data queued in output buffer or not.
function accessTelemetryPush(module, rxUid, sensorId, frameId, dataId, value) end
--- Pops a received Crossfire Telemetry packet from the queue.
---
--- **Since:** 2.2.0
---@return nil _nil #queue does not contain any (or enough) bytes to form a whole packet
---@return number|table multiple #returns 2 values:
--- command (number)
--- packet (table) data bytes
function crossfireTelemetryPop() end
--- This functions allows for sending telemetry data toward the TBS Crossfire link.
--- 
--- When called without parameters, it will only return the status of the output buffer without sending anything.
---
--- **Since:** 2.2.0
---@param command? any #command
---@param data? table #table of data bytes
---@return boolean boolean #data queued in output buffer or not.
---@return nil _nil #incorrect telemetry protocol.
function crossfireTelemetryPush(command, data) end
--- Pops a received Ghost Telemetry packet from the queue.
---
--- **Since:** 2.7.0
---@return nil _nil #queue does not contain any (or enough) bytes to form a whole packet
---@return number|table multiple #returns 2 values:
--- type (number)
--- packet (table) data bytes
function ghostTelemetryPop() end
--- This functions allows for sending telemetry data toward the Ghost link.
--- 
--- When called without parameters, it will only return the status of the output buffer without sending anything.
---
--- **Since:** 2.7.0
---@param command? any #command
---@param data? table #table of data bytes
---@return boolean boolean #data queued in output buffer or not.
---@return nil _nil #incorrect telemetry protocol.
function ghostTelemetryPush(command, data) end
--- Return the RAS value or nil if no valid hardware found
---
--- **Since:** 2.2.0
--- > **Notice:** RAS was called SWR in the past
---@return number number #representing RAS value. Value bellow 0x33 (51 decimal) are all ok, value above 0x33 indicate a hardware antenna issue.
--- This is just a hardware pass/fail measure and does not represent the quality of the radio link
function getRAS() end
---@class (exact) General_getTxGPS_Return
---@field lat number #internal GPS latitude, positive is North
---@field lon number #internal GPS longitude, positive is East
---@field numsat number #current number of sats locked in by the GPS sensor
---@field fix boolean #fix status
---@field alt number #internal GPS altitude in 0.1m
---@field speed number #internal GPSspeed in 0.1m/s
---@field heading number #internal GPS ground course estimation in degrees * 10
---@field hdop number #internal GPS horizontal dilution of precision

--- Return the internal GPS position or nil if no valid hardware found
---
--- **Since:** 2.2.2
---@return General_getTxGPS_Return table #representing the current radio position
--- `lat` (number) internal GPS latitude, positive is North
--- `lon` (number) internal GPS longitude, positive is East
--- `numsat` (number) current number of sats locked in by the GPS sensor
--- `fix` (boolean) fix status
--- `alt` (number) internal GPS altitude in 0.1m
--- `speed` (number) internal GPSspeed in 0.1m/s
--- `heading`  (number) internal GPS ground course estimation in degrees * 10
--- `hdop` (number)  internal GPS horizontal dilution of precision
function getTxGPS() end
--- Return flight mode data.
---
--- **Since:** 2.1.7
---@param mode number #flight mode number to return (0 - 8). If mode parameter
--- is not specified (or contains invalid value), then the current flight mode data is returned.
---@return number|string multiple #returns 2 values:
--- (number) (current) flight mode number (0 - 8)
--- (string) (current) flight mode name
function getFlightMode(mode) end
--- Play a file from the SD card
---
--- **Since:** 2.0.0
---@param filename string #(string) full path to wav file (i.e. "/SOUNDS/en/system/tada.wav")
--- Introduced in 2.1.0: If you use a relative path, the current language is appended
--- to the path (example: for English language: `/SOUNDS/en` is appended)
---@param volume? number #:
---  - (1..5) override radio settings Wav volume for the duration of file
---  - omitting the parameter uses radio settings Wav volume
function playFile(filename, volume) end
--- Play a numerical value (text to speech)
---
--- **Since:** 2.0.0
---@param value number #(number) number to play. Value is interpreted as integer.
---@param unit number #unit identifier [Full list]((../appendix/units.html))
---@param attributes? number #(unsigned number) possible values:
--- `0 or not present` plays integral part of the number (for a number 123 it plays 123)
--- `PREC1` plays a number with one decimal place (for a number 123 it plays 12.3)
--- `PREC2` plays a number with two decimal places (for a number 123 it plays 1.23)
---  
---  @param volume (number):
---  - (1..5) override radio settings Wav volume for the duration of file
---  - omitting the parameter uses radio settings Wav volume
--- <br>**Flag hints:** PREC1, PREC2<br>
---@param volume? any
function playNumber(value, unit, attributes, volume) end
--- Play a time value (text to speech)
---
--- **Since:** 2.1.0
---@param duration number #number of seconds to play. Only integral part is used.
---@param hourFormat? number #:
--- `0 or not present` play format: minutes and seconds.
--- `!= 0` play format: hours, minutes and seconds.
---@param volume? number #:
---  - (1..5) override radio settings Wav volume for the duration of file
---  - omitting the parameter uses radio settings Wav volume
function playDuration(duration, hourFormat, volume) end
--- Play a tone
---
--- **Since:** 2.1.0
---@param frequency number #tone frequency in Hz (from 150 to 15000)
---@param duration number #length of the tone in milliseconds
---@param pause number #length of the silence after the tone in milliseconds
---@param flags? number #(number):
--- `0 or not present` play with normal priority.
--- `PLAY_BACKGROUND` play in background (built in vario function uses this context)
--- `PLAY_NOW` play immediately
--- <br>**Flag hints:** PLAY_BACKGROUND, PLAY_NOW<br>
---@param freqIncr? number #positive number increases the tone pitch (frequency with time),
--- negative number decreases it. The frequency changes every 10 milliseconds, the change is `freqIncr * 10Hz`.
--- The valid range is from -127 to 127.
---@param volume? number #:
---  - (1..5) override radio settings Beep volume for the duration of file
---  - omitting the parameter uses radio settings Beep volume
function playTone(frequency, duration, pause, flags, freqIncr, volume) end
--- Takes a screenshot, which is saved to the SCREENSHOTS folder on the radio SD card.
---
--- **Since:** 2.11
--- > **Notice:** This command is currently not rate limited, so repeated frequent calls will slow down the UI and can even freeze the entire radio, so should be used with care.
function screenshot() end
--- Generate haptic feedback
---
--- **Since:** 2.2.0
---@param duration number #length of the haptic feedback in milliseconds
---@param pause number #length of the silence after haptic feedback in milliseconds
---@param flags? number #(number):
--- `0 or not present` play with normal priority
--- `PLAY_NOW` play immediately
--- <br>**Flag hints:** PLAY_NOW<br>
function playHaptic(duration, pause, flags) end
--- Stops key state machine. See [Key Events](../key_events.md) for the detailed description.
---
--- **Since:** 2.0.0
---@param key number #key to be killed, can also include event type (only the key part is used)
function killEvents(key) end
--- Returns gray value which can be used in LCD functions
---
--- **Since:** 2.0.13
--- > **Notice:** Only available on Taranis
---@return number number #a value that represents amount of *greyness* (from 0 to 15)
function GREY() end
---@class (exact) General_getGeneralSettings_Return
---@field battWarn number #radio battery range - warning value
---@field battMin number #radio battery range - minimum value
---@field battMax number #radio battery range - maximum value
---@field imperial number #set to a value different from 0 if the radio is set to the
---@field language string #radio language (used for menus)
---@field voice string #voice language (used for speech)
---@field gtimer number #radio global timer in seconds (does not include current session)

--- Returns (some of) the general radio settings
---
--- **Since:** 2.0.6
---@return General_getGeneralSettings_Return table #with elements:
--- `battWarn` (number) radio battery range - warning value
--- `battMin` (number) radio battery range - minimum value
--- `battMax` (number) radio battery range - maximum value
--- `imperial` (number) set to a value different from 0 if the radio is set to the
---  IMPERIAL units
--- `language` (string) radio language (used for menus)
--- `voice` (string) voice language (used for speech)
--- `gtimer` (number) radio global timer in seconds (does not include current session)
function getGeneralSettings() end
---@class (exact) General_getGlobalTimer_Return
---@field gtimer number #radio global timer in seconds
---@field session number #radio session in seconds
---@field ttimer number #radio throttle timer in seconds
---@field tptimer number #radio throttle percent timer in seconds

--- Returns radio timers
---
--- **Since:** 2.3.0
---@return General_getGlobalTimer_Return table #with elements:
--- `gtimer` (number) radio global timer in seconds
--- `session` (number) radio session in seconds
--- `ttimer` (number) radio throttle timer in seconds
--- `tptimer` (number) radio throttle percent timer in seconds
function getGlobalTimer() end
--- Raises a pop-up on screen that allows uses input
---
--- **Since:** 2.0.0
--- > **Notice:** Use only from stand-alone and telemetry scripts.
---@param title string #(string) text to display
---@param event number #the event variable that is passed in from the
--- Run function (key pressed)
---@param input number #value that can be adjusted by the +/- keys
---@param min number #min value that input can reach (by pressing the - key)
---@param max number #max value that input can reach
---@return number number #result of the input adjustment
---@return any OK #user pushed ENT key
---@return any CANCEL #user pushed EXIT key
function popupInput(title, event, input, min, max) end
--- Raises a pop-up on screen that shows a warning
---
--- **Since:** 2.2.0
--- > **Notice:** Use only from stand-alone and telemetry scripts.
---@param title string #(string) text to display
---@param event number #the event variable that is passed in from the
--- Run function (key pressed)
---@return any CANCEL #user pushed EXIT key
function popupWarning(title, event) end
--- Raises a pop-up on screen that asks for confirmation
---
--- **Since:** 2.2.0
--- > **Notice:** Use only from stand-alone and telemetry scripts.
---@param title string #(string) title to display
---@param event number #the event variable that is passed in from the
--- Run function (key pressed)
---@param message? string #text to display
---@return any CANCEL #user pushed EXIT key
function popupConfirmation(title, event, message) end
--- Get stick that is assigned to a channel. See Default Channel Order in General Settings.
---
--- **Since:** 2.0.0
---@param channel number #(number) channel number (0 means CH1)
---@return number number #Stick assigned to this channel (from 0 to 3)
function defaultStick(channel) end
--- **Since:** 2.2.0
--- > **Notice:** All three parameters `id`, `subID` and `instance` can't be zero at the same time. At least one of them
--- > must be different from zero.
---@param id any #Id of the sensor, valid range is from 0 to 0xFFFF
---@param subID any #subID of the sensor, usually 0, valid range is from 0 to 7
---@param instance any #instance of the sensor (SensorID), valid range is from 0 to 0xFF
---@param value number #fed to the sensor
---@param unit? any #unit of the sensor [Full list](../../appendix/units.html)
---@param precision? any #the precision of the sensor
--- `0 or not present` no decimal precision.
--- `!= 0` value is divided by 10^precision, e.g. value=1000, prec=2 => 10.00.
---@param name? string #(string) Name of the sensor if it does not yet exist (4 chars).
--- `not present` Name defaults to the Id.
--- `present` Sensor takes name of the argument. Argument must have name surrounded by quotes: e.g., "Name"
---@return any true_ #if the sensor was just added. In this case the value is ignored (subsequent call will set the value)
function setTelemetryValue(id, subID, instance, value, unit, precision, name) end
--- Get channel assigned to stick. See Default Channel Order in General Settings
---
--- **Since:** 2.0.0
---@param stick number #stick number (from 0 to 3)
---@return number number #channel assigned to this stick (from 0 to 3)
---@return nil _nil #stick not found
function defaultChannel(stick) end
--- flushes audio queue
---
function flushAudio() end
--- Get RSSI value as well as low and critical RSSI alarm levels (in dB)
---
--- **Since:** 2.2.0
---@return any rssi #RSSI value (0 if no link)
---@return any alarm_low #Configured low RSSI alarm level
---@return any alarm_crit #Configured critical RSSI alarm level
function getRSSI() end
--- Load a Lua script file. This is similar to Lua's own [loadfile()](https://www.lua.org/manual/5.2/manual.html#pdf-loadfile)
--- API method, but it uses OpenTx's optional pre-compilation feature to save memory and time during load.
--- 
--- Return values are same as from Lua API loadfile() method: If the script was loaded w/out errors
--- then the loaded script (or "chunk") is returned as a function. Otherwise, returns nil plus the error message.
--- ### Example
--- 
--- ```lua
---   fun, err = loadScript("/SCRIPTS/FUNCTIONS/print.lua")
---   if (fun ~= nil) then
---      fun("Hello from loadScript()")
---   else
---      print(err)
---   end
--- ```
---
--- **Since:** 2.2.0
--- > **Notice:** Note that you will get an error if you specify `mode` as "b" or "t" and that specific version of the file does not exist (eg. no .luac file when "b" is used).
--- >   Also note that `mode` is NOT passed on to Lua's loader function, so unlike with loadfile() the actual file content is not checked (as if no mode or "bt" were passed to loadfile()).
---@param file string #(string) Full path and file name of script. The file extension is optional and ignored (see `mode` param to control
---   which extension will be used). However, if an extension is specified, it should be ".lua" (or ".luac"), otherwise it is treated
---   as part of the file name and the .lua/.luac will be appended to that.
---@param mode? string #(optional) Controls whether to force loading the text (.lua) or pre-compiled binary (.luac)
---   version of the script. By default ETX will load the newest version and compile a new binary if necessary (overwriting any
---   existing .luac version of the same script, and stripping some debug info like line numbers).
---   You can use `mode` to control the loading behavior more specifically. Possible values are:
--- `b` only binary.
--- `t` only text.
--- `T` (default on simulator) prefer text but load binary if that is the only version available.
--- `bt` (default on radio) either binary or text, whichever is newer (binary preferred when timestamps are equal).
--- Add `x` to avoid automatic compilation of source file to .luac version.
---        Eg: "tx", "bx", or "btx".
--- Add `c` to force compilation of source file to .luac version (even if existing version is newer than source file).
---        Eg: "tc" or "btc" (forces "t", overrides "x").
--- Add `d` to keep extra debug info in the compiled binary.
---        Eg: "td", "btd", or "tcd" (no effect with just "b" or with "x").
---@param env? number #See documentation for Lua function loadfile().
---@return fun(...):... _function #The loaded script, or `nil` if there was an error (e.g. file not found or syntax error).
---@return string string #Error message(s), if any. Blank if no error occurred.
function loadScript(file, mode, env) end
--- Get percent of already used Lua instructions in current script execution cycle.
---
--- **Since:** 2.2.1
---@return number usage #a value from 0 to 100 (percent)
function getUsage() end
--- Get available memory remaining in the Heap for Lua.
---
---@return number usage #a value returned in b
function getAvailableMemory() end
--- Resets the radio global timer to 0.
---
--- **Since:** 2.2.2
---@param type? any
---@param optional? any #: if set to 'all', throttle ,throttle percent and session timers are reset too
---                     if set to 'session', radio session timer is reset too
---                     if set to 'ttimer', radio throttle timer is reset too
---                     if set to  'tptimer', radio throttle percent timer is reset too
function resetGlobalTimer(type, optional) end
--- This function reads/writes the Multi protocol buffer to interact with a protocol.
---
--- **Since:** 2.3.2
---@param address any #to read/write in the buffer
---@param value? number
---@param optional? any #value to write in the buffer
---@return number buffer #value (number)
function multiBuffer(address, value, optional) end
--- **Since:** 2.3.12
---@param baudrate any #Desired baurate
--- 
--- Set baudrate for serial port(s) affected to LUA
function setSerialBaudrate(baudrate) end
--- **Since:** 2.3.10
---@param str string #String to be written to the serial port.
--- 
--- Writes a string to the serial port. The string is allowed to contain any character, including 0.
function serialWrite(str) end
--- **Since:** 2.3.8
---@param num? number|string #(optional): maximum number of bytes to read.
---                        If non-zero, serialRead will read up to num characters from the buffer.
---                        If 0 or left out, serialRead will read up to and including the first newline character or the end of the buffer.
---                        Note that the returned string may not end in a newline if this character is not present in the buffer.
---@return string str #string. Empty if no new characters were available.
--- 
--- Reads characters from the serial port. The string is allowed to contain any character, including 0.
function serialRead(num) end
--- **Since:** 2.9.0
---@param port_nr any #valid values are only 0 and 1 on radios that have SWSERIALPOWER defined
---                 0 - first serial port, e.g. on TX16S AUX1
---                 1 - second serial port, e.g. on TX16S AUX2
---@return number value #true for power enabled, false for power disabled.
---@return nil _nil #the serial port power control not available on this radio
function serialGetPower(port_nr) end
--- **Since:** 2.9.0
---@param port_nr any #valid values are only 0 and 1 on radios that have SWSERIALPOWER defined
---                 0 - first serial port, e.g. on TX16S AUX1
---                 1 - second serial port, e.g. on TX16S AUX2
---@param value number #0 - disable power
---               1 - enable power
---@return boolean success #true/false.
function serialSetPower(port_nr, value) end
--- **Since:** 2.6
--- > **Notice:** Only available on radios with color display
---@param id number #integer between 1 and 16 identifying the shared memory variable.
---@param value number #integer. The value of the shared memory variable.
--- 
--- Sets the value of a shared memory variable that can be used for passing data between Lua widgets and other Lua scripts.
function setShmVar(id, value) end
--- **Since:** 2.6
--- > **Notice:** Only available on radios with color display
---@param id number #integer between 1 and 16 identifying the shared memory variable.
---@return number value #integer. The value of the shared memory variable.
--- 
--- Gets the value of a shared memory variable that can be used for passing data between Lua widgets and other Lua scripts.
function getShmVar(id) end
--- **Since:** 2.6
---@param id number #integer identifying the sticky logical switch (zero for LS1 etc.).
---@param value number #true/false. The new value of the sticky logical switch.
---@return boolean|fun(...):... bufferFull #true/false. This function sends a message from Lua to the logical switch processor
--- via a buffer with eight slots that are read 10 times per second. If the buffer is full, then a true value
--- is returned and no messages was sent (i.e. the switch was not changed).
--- 
--- Sets the value of a sticky logical switch.
function setStickySwitch(id, value) end
--- **Since:** 2.6
---@param id number #integer identifying the logical switch (zero for LS1 etc.).
---@return number value #true/false.
--- 
--- Reads the value of a logical switch.
function getLogicalSwitchValue(id) end
---@class (exact) General_getSwitchInfo_Return
---@field type number #field identifier
---@field isCustomisableSwitch boolean #field identifier
---@field name string #switch name

--- **Since:** 2.12
---@param sourceIndex number #integer identifying a value source as returned by `getSourceIndex(sourceName)` or the `id` field in the table returned by `getFieldInfo`.
---@return General_getSwitchInfo_Return table #information about requested field, table elements:
--- `type`   (number) field identifier
--- 0 = SWITCH_NONE
--- 1 = SWITCH_TOGGLE
--- 2 = SWITCH_2POS
--- 3 = SWITCH_3POS
--- 
--- `isCustomisableSwitch`   (boolean) field identifier
--- return true if switch is a customisable switch
--- 
--- `name` (string) switch name
function getSwitchInfo(sourceIndex) end
--- **Since:** 2.6
---@param positionName string #string naming a switch position as it is shown on radio menus where you can select a switch. Notice that many names have
--- special characters in them like arrow up/down etc.
---@return number value #integer. The switchIndex, which can be used as input for `getSwitchName(switchIndex)` and `getSwitchValue(switchIndex)`. Also corresponds
--- to the fields in the table returned by `model.getLogicalSwitch(switch)` identifying switches.
function getSwitchIndex(positionName) end
--- **Since:** 2.6
---@param switchIndex number #integer identifying a switch as returned by `getSwitchIndex(positionName)` or fields in the table returned by
--- `model.getLogicalSwitch(switch)` identifying switches.
---@return number value #string naming the switch position as it is shown on radio menus where a switch can be chosen.
function getSwitchName(switchIndex) end
--- **Since:** 2.6
---@param switchIndex number #integer identifying a switch as returned by `getSwitchIndex(positionName)` or fields in the table returned by
--- `model.getLogicalSwitch(switch)` identifying switches.
---@return number value #true/false. The value of the switch.
function getSwitchValue(switchIndex) end
--- **Since:** 2.6
---@param first? number|nil #the first switch index. If `nil` or omitted, the first available switch is used.
---@param last? number|fun(...):...|nil #the last switch index. If `nil` or omitted, the last available switch is used.
--- 
--- This is an iterator function over switch positions. `for switchIndex, switchName in switches() do ...` will iterate over all available switch positions.
function switches(first, last) end
--- **Since:** 2.6
--- > **Notice:** the source names shown on the screen are not the same as the names used by `getFieldInfo` and `getValue`. But the indices are the same, so `getValue(index)` will work with the indices obtained here.
--- > This function is rather time consuming, and should not be used repeatedly in a script, if it can be avoided.
---@param sourceName string #string naming a value source as it is shown on radio menus where you can select it. Notice that many names have special characters in them.
---@return number sourceIndex #integer. The source index, which can be used as input for `getSourceName(sourceIndex)`, `getValue(sourceIndex)`, and `getFieldInfo(sourceIndex)`.
function getSourceIndex(sourceName) end
--- **Since:** 2.6
--- > **Notice:** the source names shown on the screen are not the same as the names used by `getFieldInfo` and `getValue`. But the indices are the same, so `getValue(index)` will work with the indices used here.
---@param sourceIndex number #integer identifying a value source as returned by `getSourceIndex(sourceName)` or the `id` field in the table returned by `getFieldInfo`.
---@return string sourceName #string naming the value source as it is shown on radio menus where a source can be chosen.
function getSourceName(sourceIndex) end
--- **Since:** 2.6
---@param first? number|nil #the first source index. If `nil` or omitted, the first available source is used.
---@param last? number|fun(...):...|nil #the last soure index. If `nil` or omitted, the last available source is used.
--- 
--- This is an iterator function over value sources. `for sourceIndex, sourceName in sources() do ...` will iterate over all available value sources.
function sources(first, last) end
--- **Since:** 2.8.0
---@param outputIndex number #integer identifying the output channel number 0 for CH1, up to MAX_OUTPUT_CHANNELS - 1.
--- <br>**Flag hints:** MAX_OUTPUT_CHANNELS<br>
---@return number value #current output value (number). Zero is returned for:
--- non-existing outputs
function getOutputValue(outputIndex) end
--- **Since:** 2.9.0
---@return number value #current output value (number).
---  0 - Not Connected
---  1 - Connected
---  2 - Disconnected
---  3 - Reconnected
function getTrainerStatus() end
--- **Since:** 2.10
---@param id number #integer identifying a led in the led chain
---@param rvalue any #interger, value of red channel
---@param bvalue any #interger, value of blue channel
---@param cvalue any
---@param gvalue? any #interger, value of green channel
---@return boolean|number #true if LED index is valid, false otherwise
function setRGBLedColor(id, rvalue, bvalue, cvalue, gvalue) end
--- Overrides the LED color for a custom function switch
---   - if passed 4 arguments (id, r, g, b) then sets override color
---   - if passed 1 argument (id) then cancels override color
---
--- **Since:** 3.0.0
---@param id string|fun(...):... #string identifying a custom function switch name
---@param rvalue any #interger, value of red channel
---@param bvalue any #interger, value of blue channel
---@param cvalue any
---@param gvalue? any #interger, value of green channel
---@return boolean|number #true if LED index is valid, false otherwise
function setCFSLedColor(id, rvalue, bvalue, cvalue, gvalue) end
--- Apply RGB led colors previously defined by setRGBLedColor
---
--- **Since:** 2.10
function applyRGBLedColors() end
--- **Since:** 3.0
---@return number #integer, a 1 to 4 value corresponding to radio stick mode
function getStickMode() end
--- **Since:** 3.0
---@param offset number #integer, offset in angular degree. -1 to offset to current X position
---@param range number #integer, range in angular degree. 180° max.90 means min/max value will be reached at 45° from offset position
function setIMU_X(offset, range) end
--- **Since:** 3.0
---@param offset number #integer, offset in angular degree. -1 to offset to current Y position
---@param range number #integer, range in angular degree, 180° max. 90 means min/max value will be reached at 45° from offset position
function setIMU_Y(offset, range) end
