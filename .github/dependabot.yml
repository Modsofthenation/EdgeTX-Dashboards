# Dependabot version updates for this npm workspaces monorepo + Tauri desktop.
# Docs: https://docs.github.com/en/code-security/dependabot/dependabot-version-updates
#
# Do not set `labels` until those labels exist on the repo. Missing labels
# make Dependabot comment on every PR instead of tagging it.
version: 2
updates:
  # Root package-lock.json covers apps/* and packages/* workspaces.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    groups:
      npm-minor-and-patch:
        applies-to: version-updates
        update-types:
          - minor
          - patch

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      github-actions:
        applies-to: version-updates
        patterns:
          - "*"

  - package-ecosystem: cargo
    directory: /apps/desktop/src-tauri
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      cargo-minor-and-patch:
        applies-to: version-updates
        update-types:
          - minor
          - patch
