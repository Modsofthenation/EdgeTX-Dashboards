CURSOR_API_KEY=

# Optional: enable Cursor SDK local sandbox (Linux/macOS with sandbox support only)
# CURSOR_SANDBOX_ENABLED=true

# Optional: require this secret on API routes (Authorization: Bearer <secret> or X-Generator-Secret header)
# Set when exposing the web UI beyond localhost.
GENERATOR_API_SECRET=
