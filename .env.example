# Cursor cloud agent API key (required for generation)
CURSOR_API_KEY=

# Optional: override SQLite chat DB and runtime data directory (default: <repo>/data)
WIDGET_GEN_DATA_DIR=

# Optional: API route auth token (if configured in apiSecurity)
# WIDGET_GEN_API_TOKEN=
