import type { PrefabSection } from "./types.ts";

/**
 * TX15-adapted modular sections distilled from StacyDashV4.
 *
 * Original StacyDash targets 800×480 with LVGL. These prefabs keep the same
 * information architecture on 480×320 using lcd.* so preview + editor work.
 *
 * Sensor names follow this repo's Rotorflight catalog (HSpd, EscT, RQLY, …).
 * StacyDash / rf2bg may discover aliases (Hspd, Tesc, RQly) — see telemetryNotes.
 */
export const STACYDASH_ROTORFLIGHT_PREFABS: PrefabSection[] = [
  {
    id: "rf-topbar-link",
    label: "RF top bar + link",
    shortLabel: "TB",
    description:
      "StacyDash-style header: title, flight timer slot, and 4-block RQLY signal bars",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "header",
    requiredSensors: ["RQLY"],
    optionalSensors: ["1RSS"],
    telemetryNotes: [
      "Needs CRSF link telemetry (RQLY). StacyDash also accepts RQly / LQ aliases.",
      "Timer uses EdgeTX model timer 1 when present; shows 0:00 otherwise.",
    ],
    createSrcBindings: {
      rqly: "RQLY",
      rss1: "1RSS",
    },
    srcSlotLabels: {
      rqly: "Link quality",
      rss1: "RSSI 1",
    },
    defaultBounds: { x: 0, y: 0, w: 480, h: 36 },
    refreshLines: [
      "local rqlyTop = telem(widget.src.rqly)",
      'local rqlyTopStr = rqlyTop > 0 and (tostring(math.floor(rqlyTop + 0.5)) .. "%") or "---"',
      "local t1 = model.getTimer(1)",
      "local tSecs = (t1 and t1.value) and math.floor(math.abs(t1.value)) or 0",
      'local timerStr = string.format("%d:%02d", math.floor(tSecs / 60), tSecs % 60)',
      "lcd.drawFilledRectangle(0, 0, 480, 36, GREY)",
      'lcd.drawText(12, 10, "HELI", SMLSIZE + WHITE)',
      "lcd.drawText(200, 8, timerStr, MIDSIZE + WHITE)",
      "local sigX = 360",
      "local sigY = 22",
      "local activeBars = math.floor((math.max(0, math.min(100, rqlyTop)) + 24) / 25)",
      "lcd.drawFilledRectangle(sigX, sigY - 6, 6, 6, rqlyTop > 0 and activeBars >= 1 and RED or DARKGREY)",
      "lcd.drawFilledRectangle(sigX + 10, sigY - 10, 6, 10, rqlyTop > 0 and activeBars >= 2 and ORANGE or DARKGREY)",
      "lcd.drawFilledRectangle(sigX + 20, sigY - 14, 6, 14, rqlyTop > 0 and activeBars >= 3 and YELLOW or DARKGREY)",
      "lcd.drawFilledRectangle(sigX + 30, sigY - 18, 6, 18, rqlyTop > 0 and activeBars >= 4 and GREEN or DARKGREY)",
      "lcd.drawText(430, 10, rqlyTopStr, SMLSIZE + WHITE)",
    ],
  },
  {
    id: "rf-model-panel",
    label: "RF model panel",
    shortLabel: "MD",
    description:
      "Left model card with placeholder image area and flight-count footer (StacyDash pic panel)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "panel",
    requiredSensors: [],
    optionalSensors: [],
    telemetryNotes: [
      "Optional model PNG: assign under Model Setup → Bitmap, or place /IMAGES/<ModelName>.png matching RF Configurator name exactly.",
      "Flight count is a display placeholder here — StacyDash persists flights-count.csv at SD root (not generated by default).",
    ],
    createSrcBindings: {},
    defaultBounds: { x: 12, y: 48, w: 156, h: 120 },
    refreshLines: [
      "lcd.drawFilledRectangle(12, 48, 156, 120, DARKGREY)",
      "lcd.drawRectangle(12, 48, 156, 120, GREY)",
      'lcd.drawText(20, 56, "MODEL", SMLSIZE + GREY)',
      "if widget.modelBmp and widget.bmpW and widget.bmpW > 0 then",
      "  lcd.drawBitmap(widget.modelBmp, 20, 72)",
      "else",
      "  lcd.drawFilledRectangle(20, 72, 140, 70, BLACK)",
      '  lcd.drawText(90, 98, "IMG", SMLSIZE + CENTER + GREY)',
      "end",
      "lcd.drawFilledRectangle(20, 148, 140, 1, GREY)",
      'lcd.drawText(90, 152, "0 Flights", SMLSIZE + CENTER + WHITE)',
    ],
  },
  {
    id: "rf-governor-card",
    label: "RF governor card",
    shortLabel: "GV",
    description:
      "Governor / flight-mode status tile (StacyDash GOVERNOR panel)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "card",
    requiredSensors: ["FM"],
    optionalSensors: ["Gov"],
    telemetryNotes: [
      "Prefers FM (flight mode). Rotorflight custom CRSF may also publish Gov (governor enum) via rf2bg — enable Discover new after rf2bg is running.",
      "StacyDash maps Gov states OFF/IDLE/SPOOLUP/ACTIVE/… — this TX15 card shows the FM string (or Gov when bound).",
    ],
    createSrcBindings: {
      fm: "FM",
      gov: "Gov",
    },
    srcSlotLabels: {
      fm: "Flight mode",
      gov: "Governor",
    },
    defaultBounds: { x: 12, y: 180, w: 156, h: 72 },
    refreshLines: [
      "local govRaw = telem(widget.src.gov)",
      "local fmRaw = widget.src.fm and getValue(widget.src.fm) or nil",
      'local govStr = "--"',
      'if type(fmRaw) == "string" and #fmRaw > 0 then govStr = fmRaw',
      "elseif govRaw and govRaw ~= 0 then govStr = tostring(math.floor(govRaw))",
      "end",
      "lcd.drawFilledRectangle(12, 180, 156, 72, DARKGREY)",
      "lcd.drawRectangle(12, 180, 156, 72, GREY)",
      'lcd.drawText(90, 188, "GOVERNOR", SMLSIZE + CENTER + GREY)',
      "lcd.drawText(90, 208, govStr, MIDSIZE + CENTER + WHITE)",
    ],
  },
  {
    id: "rf-headspeed-hero",
    label: "RF headspeed hero",
    shortLabel: "HS",
    description:
      "Large headspeed readout with max/tail secondaries (StacyDash hero)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "hero",
    requiredSensors: ["HSpd"],
    optionalSensors: ["RPM", "Tspd"],
    telemetryNotes: [
      "Requires custom CRSF headspeed: enable rf2bg Special Function (Repeat: On), then Telemetry → Discover new.",
      "Catalog name is HSpd; some radios discover Hspd — both refer to Rotorflight headspeed.",
      "Optional Tspd (tail RPM) appears when published by the RF custom telemetry set.",
    ],
    createSrcBindings: {
      hspd: "HSpd",
      rpm: "RPM",
      tspd: "Tspd",
    },
    srcSlotLabels: {
      hspd: "Headspeed",
      rpm: "RPM fallback",
      tspd: "Tail RPM",
    },
    defaultBounds: { x: 180, y: 48, w: 288, h: 100 },
    refreshLines: [
      "local hspd = telem(widget.src.hspd)",
      "local tspd = telem(widget.src.tspd)",
      'local hspdStr = hspd > 0 and tostring(math.floor(hspd + 0.5)) or "--"',
      'local tspdStr = tspd > 0 and tostring(math.floor(tspd + 0.5)) or "--"',
      "lcd.drawFilledRectangle(180, 48, 288, 100, DARKGREY)",
      "lcd.drawRectangle(180, 48, 288, 100, GREY)",
      'lcd.drawText(192, 56, "HEADSPEED RPM", SMLSIZE + GREY)',
      "lcd.drawText(192, 78, hspdStr, DBLSIZE + WHITE)",
      'lcd.drawText(360, 72, "tail", SMLSIZE + GREY)',
      "lcd.drawText(456, 72, tspdStr, SMLSIZE + RIGHT + WHITE)",
      'lcd.drawText(360, 96, "rpm", SMLSIZE + GREY)',
    ],
  },
  {
    id: "rf-motor-tiles",
    label: "RF motor tiles",
    shortLabel: "MT",
    description:
      "Four metric tiles: AMPS · CELL · BEC · ESC T (StacyDash tile row)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "strip",
    requiredSensors: ["Curr", "RxBt", "EscT"],
    optionalSensors: ["Vbec", "Vcel", "MotT"],
    telemetryNotes: [
      "AMPS uses Curr. CELL prefers Vcel when present, else approximates from RxBt/Vbat.",
      "BEC uses Vbec (custom CRSF via rf2bg). ESC T uses EscT (StacyDash alias: Tesc).",
      "Nitro setups may hide AMPS/ESC and show receiver pack cell voltage instead.",
    ],
    createSrcBindings: {
      curr: "Curr",
      rxbt: "RxBt",
      vcel: "Vcel",
      vbec: "Vbec",
      esct: "EscT",
      mott: "MotT",
    },
    srcSlotLabels: {
      curr: "Current (AMPS)",
      rxbt: "Pack voltage",
      vcel: "Cell voltage",
      vbec: "BEC voltage",
      esct: "ESC temp",
      mott: "Motor temp",
    },
    defaultBounds: { x: 180, y: 160, w: 288, h: 72 },
    refreshLines: [
      "local amps = telem(widget.src.curr)",
      "local vcel = telem(widget.src.vcel)",
      "local vpack = telem(widget.src.rxbt)",
      "local vbec = telem(widget.src.vbec)",
      "local esct = telem(widget.src.esct)",
      'local ampsStr = amps > 0 and tostring(math.ceil(amps)) or "--"',
      "local cellV = vcel > 0 and vcel or (vpack > 0 and vpack / 6 or 0)",
      'local cellStr = cellV > 0 and string.format("%.2f", cellV) or "--"',
      'local becStr = vbec > 0 and string.format("%.1f", vbec) or "--"',
      'local escStr = esct > 0 and tostring(math.floor(esct + 0.5)) or "--"',
      "lcd.drawFilledRectangle(180, 160, 288, 72, DARKGREY)",
      "lcd.drawRectangle(180, 160, 288, 72, GREY)",
      "lcd.drawFilledRectangle(250, 168, 1, 56, GREY)",
      "lcd.drawFilledRectangle(322, 168, 1, 56, GREY)",
      "lcd.drawFilledRectangle(394, 168, 1, 56, GREY)",
      'lcd.drawText(186, 168, "AMPS", SMLSIZE + GREY)',
      "lcd.drawText(186, 188, ampsStr, MIDSIZE + WHITE)",
      'lcd.drawText(258, 168, "CELL", SMLSIZE + GREY)',
      "lcd.drawText(258, 188, cellStr, MIDSIZE + WHITE)",
      'lcd.drawText(330, 168, "BEC", SMLSIZE + GREY)',
      "lcd.drawText(330, 188, becStr, MIDSIZE + WHITE)",
      'lcd.drawText(402, 168, "ESC T", SMLSIZE + GREY)',
      "lcd.drawText(402, 188, escStr, MIDSIZE + WHITE)",
    ],
  },
  {
    id: "rf-battery-bar",
    label: "RF battery bar",
    shortLabel: "BB",
    description:
      "Full-width battery % bar with pack header (StacyDash bottom fuel bar)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "footer",
    requiredSensors: ["Bat%", "RxBt"],
    optionalSensors: ["Capa", "Vbat"],
    telemetryNotes: [
      "Bat% is Rotorflight Smart Fuel charge level when enabled on the FC.",
      "Header shows pack voltage (RxBt / Vbat) and used mAh (Capa) when available.",
      "StacyDash also supports a Nitro RX-pack mode with configurable min/max volts — not included in this TX15 prefab.",
    ],
    createSrcBindings: {
      batp: "Bat%",
      rxbt: "RxBt",
      capa: "Capa",
      vbat: "Vbat",
    },
    srcSlotLabels: {
      batp: "Battery %",
      rxbt: "Pack voltage (RxBt)",
      capa: "Capacity used",
      vbat: "Pack voltage (Vbat)",
    },
    defaultBounds: { x: 12, y: 248, w: 456, h: 60 },
    refreshLines: [
      "local batp = telem(widget.src.batp)",
      "local volts = telem(widget.src.vbat)",
      "if volts <= 0 then volts = telem(widget.src.rxbt) end",
      "local capa = telem(widget.src.capa)",
      "local pct = math.max(0, math.min(100, batp))",
      'local header = "BATTERY"',
      'if volts > 0 then header = header .. string.format(" · %.1fV", volts) end',
      'if capa > 0 then header = header .. string.format(" · %d mAh used", math.floor(capa)) end',
      "local barColor = GREEN",
      "if pct < 20 then barColor = RED elseif pct < 50 then barColor = YELLOW end",
      'local pctStr = batp > 0 and (tostring(math.floor(pct + 0.5)) .. "%") or "NO DATA"',
      "lcd.drawText(12, 248, header, SMLSIZE + GREY)",
      "lcd.drawFilledRectangle(12, 264, 456, 32, DARKGREY)",
      "lcd.drawRectangle(12, 264, 456, 32, GREY)",
      "local fillW = math.floor(452 * pct / 100)",
      "if batp > 0 and fillW > 0 then",
      "  lcd.drawFilledRectangle(14, 266, fillW, 28, barColor)",
      "end",
      "lcd.drawText(240, 272, pctStr, SMLSIZE + CENTER + WHITE)",
    ],
  },
  {
    id: "rf-nitro-pack-tiles",
    label: "RF nitro pack tiles",
    shortLabel: "NP",
    description:
      "Nitro metric tiles: RX V · CELL · BEC (no AMPS/ESC — StacyDash nitro)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "strip",
    requiredSensors: ["RxBt"],
    optionalSensors: ["Vcel", "Vbec", "Vbat"],
    telemetryNotes: [
      "Nitro / OMP: hide AMPS and ESC temp; show receiver pack voltage tiles.",
      "CELL prefers Vcel (rf2bg); else approximates from RxBt.",
      "BEC uses Vbec when published by Discover new.",
    ],
    createSrcBindings: {
      rxbt: "RxBt",
      vcel: "Vcel",
      vbec: "Vbec",
      vbat: "Vbat",
    },
    srcSlotLabels: {
      rxbt: "RX pack voltage",
      vcel: "Cell voltage",
      vbec: "BEC voltage",
      vbat: "Pack voltage (Vbat)",
    },
    defaultBounds: { x: 180, y: 160, w: 288, h: 72 },
    refreshLines: [
      "local vpack = telem(widget.src.vbat)",
      "if vpack <= 0 then vpack = telem(widget.src.rxbt) end",
      "local vcel = telem(widget.src.vcel)",
      "local vbec = telem(widget.src.vbec)",
      "local cellV = vcel > 0 and vcel or (vpack > 0 and vpack / 6 or 0)",
      'local rxStr = vpack > 0 and string.format("%.1f", vpack) or "--"',
      'local cellStr = cellV > 0 and string.format("%.2f", cellV) or "--"',
      'local becStr = vbec > 0 and string.format("%.1f", vbec) or "--"',
      "lcd.drawFilledRectangle(180, 160, 288, 72, DARKGREY)",
      "lcd.drawRectangle(180, 160, 288, 72, GREY)",
      "lcd.drawFilledRectangle(274, 168, 1, 56, GREY)",
      "lcd.drawFilledRectangle(368, 168, 1, 56, GREY)",
      'lcd.drawText(186, 168, "RX V", SMLSIZE + GREY)',
      "lcd.drawText(186, 188, rxStr, MIDSIZE + WHITE)",
      'lcd.drawText(282, 168, "CELL", SMLSIZE + GREY)',
      "lcd.drawText(282, 188, cellStr, MIDSIZE + WHITE)",
      'lcd.drawText(376, 168, "BEC", SMLSIZE + GREY)',
      "lcd.drawText(376, 188, becStr, MIDSIZE + WHITE)",
    ],
  },
  {
    id: "rf-nitro-rx-bar",
    label: "RF nitro RX pack bar",
    shortLabel: "NX",
    description:
      "Full-width RX pack voltage bar (nitro StacyDash fuel substitute)",
    family: "stacydash-rotorflight",
    protocol: "rotorflight",
    category: "footer",
    requiredSensors: ["RxBt"],
    optionalSensors: ["Vbat"],
    telemetryNotes: [
      "Nitro boards often lack Bat%/Smart Fuel — show RX pack volts instead.",
      "Bar fill maps RxBt/Vbat across a 6.0–8.4 V (2S) or scaled pack range.",
    ],
    createSrcBindings: {
      rxbt: "RxBt",
      vbat: "Vbat",
    },
    srcSlotLabels: {
      rxbt: "RX pack (RxBt)",
      vbat: "Pack (Vbat)",
    },
    defaultBounds: { x: 12, y: 248, w: 456, h: 60 },
    refreshLines: [
      "local volts = telem(widget.src.vbat)",
      "if volts <= 0 then volts = telem(widget.src.rxbt) end",
      "local vmin = 6.0",
      "local vmax = 8.4",
      "if volts > 10 then vmin = 18.0 vmax = 25.2 end",
      "local pct = 0",
      "if volts > 0 then pct = math.max(0, math.min(100, (volts - vmin) / (vmax - vmin) * 100)) end",
      'local header = "RX PACK"',
      'if volts > 0 then header = header .. string.format(" · %.1fV", volts) end',
      "local barColor = GREEN",
      "if pct < 20 then barColor = RED elseif pct < 50 then barColor = YELLOW end",
      'local pctStr = volts > 0 and string.format("%.1fV", volts) or "NO DATA"',
      "lcd.drawText(12, 248, header, SMLSIZE + GREY)",
      "lcd.drawFilledRectangle(12, 264, 456, 32, DARKGREY)",
      "lcd.drawRectangle(12, 264, 456, 32, GREY)",
      "local fillW = math.floor(452 * pct / 100)",
      "if volts > 0 and fillW > 0 then",
      "  lcd.drawFilledRectangle(14, 266, fillW, 28, barColor)",
      "end",
      "lcd.drawText(240, 272, pctStr, SMLSIZE + CENTER + WHITE)",
    ],
  },
];

export function listStacyDashPrefabIds(): string[] {
  return STACYDASH_ROTORFLIGHT_PREFABS.map((p) => p.id);
}
